# Apache server

TissUUmaps projects can be exported into static webpages, that can be uploaded to any Apache server.

1. Save your project from TissUUmaps (`menu > File > Save project`)
2. Export to static page (`menu > File > Export to static webpage`)
3. Copy the exported folder on your Apache server